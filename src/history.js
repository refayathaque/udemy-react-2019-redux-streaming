import { createBrowserHistory } from 'history';
// `history` is a separate library that comes packaged with react-router-dom, it is a react-router-dom dependency
// createBrowserHistory is a function we can call to create a new history object

export default createBrowserHistory();
